cd folder: navigate to folder

diff ~u old filename new filename: compare files

git log: shows log of commits

git diff id1 id2: finds differences in two commits

// one commit per logical change

git log —stat: give statistics on what has changed

press q to quit git log

can just enter the first 4 digit of git ID

git config --global color.ui auto

git checkout id: checkout a file

ls -a: also shows hidden files

git init: (in the folder containing the files) initialises a repository

git status: give status of a repository


